# DeepSeek Assistant 插件 - 使用教程

一个在 **Android Studio** 里直接使用 **DeepSeek 大模型** 的插件。

## 功能一览

1. **侧边栏聊天面板**：随时向 DeepSeek 提问，支持多轮对话
2. **选中代码右键操作**（三个）：
   - `DeepSeek: 解释代码` — 解释选中代码的作用和逻辑
   - `DeepSeek: 优化代码` — 优化代码的可读性/性能/健壮性
   - `DeepSeek: 添加注释` — 为选中代码添加中文注释

---

## 一、安装插件

### 方式 A：直接安装打包好的 zip（推荐）

1. 打开 Android Studio
2. 菜单 `File → Settings → Plugins`
3. 点击齿轮图标 ⚙️ → `Install Plugin from Disk...`
4. 选择打包好的文件：
   ```
   D:\code\Android\DeepSeekPlugin\build\distributions\deepseek-plugin-1.0.0.zip
   ```
5. 点击 `OK`，**重启 Android Studio**

### 方式 B：从源码重新打包

```bash
cd D:\code\Android\DeepSeekPlugin
gradlew.bat buildPlugin
# 产物在 build\distributions\deepseek-plugin-1.0.0.zip
```

---

## 二、配置 API Key（关键！）

插件通过**环境变量 `DEEPSEEK_API_KEY`** 读取密钥，需要先设置：

### 1. 获取 API Key

访问 DeepSeek 开放平台申请：<https://platform.deepseek.com/>
（注册 → 创建 API Key，形如 `sk-xxxxxxxxxxxxxxxx`）

### 2. 设置环境变量（Windows）

打开 **PowerShell 或 CMD**，执行：

```powershell
setx DEEPSEEK_API_KEY "sk-你的真实Key"
```

> ⚠️ `setx` 只对**之后新启动的进程**生效，所以必须**重启 Android Studio**。

### 3. 验证是否生效

设置页查看：`File → Settings → Tools → DeepSeek Assistant`

- 显示 `✅ 已设置（sk-...xxxx）` = 成功
- 显示 `❌ 未设置` = 环境变量没配好，检查是否重启了 AS

### 可选：指定模型

默认使用 `deepseek-chat`。想换模型（如 `deepseek-reasoner`），再设一个环境变量：

```powershell
setx DEEPSEEK_MODEL "deepseek-reasoner"
```

---

## 三、使用方式

### 1. 聊天面板

- 打开：右侧边栏的 **DeepSeek Chat** 标签页
- 输入问题，按 `Ctrl + Enter` 发送（或点「发送」按钮）
- 支持多轮对话，点「清空」重置上下文

### 2. 代码右键操作

1. 在编辑器里**选中一段代码**
2. **右键** → 找到 `DeepSeek` 菜单
3. 选择：
   - `DeepSeek: 解释代码`
   - `DeepSeek: 优化代码`
   - `DeepSeek: 添加注释`
4. 结果会弹出在对话框里展示

---

## 四、常见问题

| 问题 | 原因 | 解决 |
|------|------|------|
| 提示「未设置 DEEPSEEK_API_KEY」 | 环境变量没配/没重启 | 配好 env 后重启 AS |
| 请求失败 401 | Key 无效 | 检查 Key 是否复制完整 |
| 请求失败 429 | 限流/额度不足 | 稍后再试或充值 |
| 结果乱码 | 插件内是中文渲染 | 属正常，不影响功能 |
| 插件列表里没有 | 没重启 | 安装后必须重启 AS |

---

## 五、技术说明（给开发者）

- **语言**：Kotlin（IntelliJ 平台插件标准）
- **平台**：Android Studio build 253（IntelliJ 2025.3）
- **API 调用**：JDK 自带 `java.net.http.HttpClient` + 内置 `gson`（已打包进插件，无需外部依赖）
- **接口**：`POST https://api.deepseek.com/chat/completions`（OpenAI 兼容格式）
- **线程**：网络请求走 `executeOnPooledThread` 后台线程，UI 更新回 `invokeLater` 主线程
- **错误处理**：捕获 Throwable 并写日志（idea.log），不会静默失败

### 源码结构

```
src/main/
├── kotlin/com/example/deepseek/
│   ├── ChatToolWindowFactory.kt        # 聊天面板
│   ├── DeepSeekSettingsConfigurable.kt # 设置页
│   ├── service/DeepSeekClient.kt       # API 客户端
│   └── action/
│       ├── BaseCodeAction.kt           # 右键操作基类
│       ├── ExplainAction.kt            # 解释
│       ├── OptimizeAction.kt           # 优化
│       └── CommentAction.kt            # 注释
└── resources/META-INF/plugin.xml       # 插件注册表
```

---

## 六、API Key 安全提醒

- Key 存在环境变量里，**不会**写进插件、不会上传
- 不要在代码/截图里泄露 Key
- 泄露后到 DeepSeek 平台**重置 Key**
